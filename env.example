# Copy this file to .env and fill in real values.
# .env must be in .gitignore — never commit real credentials.

# Azure OpenAI (v1 / OpenAI-compatible endpoint)
AZURE_OPENAI_ENDPOINT=https://******.services.ai.azure.com/openai/v1
AZURE_OPENAI_API_KEY=7iy****************
AZURE_OPENAI_DEPLOYMENT=gpt-5.6-luna
AZURE_OPENAI_API_VERSION=2026-07-09

AZURE_OPENAI_BASE_URL=

# Redis Cloud connection string
# Use rediss:// (TLS) if your Redis Cloud endpoint requires it — check your dashboard.
REDIS_URL=
